name: vraag3
on: push
jobs:
  download:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout code
        uses: actions/checkout@master
      - name: Update diagram
        uses: githubocto/repo-visualizer@0.7.1
        with:
          output_file: "./wwwroot/images/diagram.svg"
          excluded_paths: "dist,node_modules"
      - name: Update diagram
        id: make_diagram
        uses: githubocto/repo-visualizer@0.7.1
      
